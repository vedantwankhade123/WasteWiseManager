// This file is needed for Vercel to detect this as a Vite app
module.exports = {
  // Your Vite config
  // This tells Vercel to treat this as a static site
  output: 'export',
  // Optional: Add a trailing slash to all paths `/about` -> `/about/`
  trailingSlash: true,
}
